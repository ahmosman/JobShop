#include <iostream>
#include <fstream>
#include <vector>

using namespace std;



int main() {


   /* cout << "Czasy trwania operacji:" << endl;
    for (int i = 0; i < num_tasks; ++i) {
        for (int j = 0; j < num_machines; ++j) {
            cout << operations_duration[i][j] << " ";
        }
        cout << endl;
    }

    cout << "Numery maszyn dla operacji:" << endl;
    for (int i = 0; i < num_tasks; ++i) {
        for (int j = 0; j < num_machines; ++j) {
            cout << operations_machines[i][j] << " ";
        }
        cout << endl;
    }*/

    // Zamykanie pliku
    //file.close();

    return 0;
}